# Models

Inherit this mixin to give your SQLAlchemy user model the columns crudauth expects. If your
table differs, satisfy the logical contract with `column_map=` instead.

::: crudauth.models.AuthUserMixin
