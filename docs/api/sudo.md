# Sudo

Short-lived re-authentication for sensitive actions. Configure with `sudo=SudoConfig()` on
[CRUDAuth](crud-auth.md) and gate routes with `auth.require_sudo()`.

::: crudauth.sudo.SudoConfig

::: crudauth.sudo.SudoManager
