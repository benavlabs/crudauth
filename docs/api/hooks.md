# Hooks

Lifecycle callbacks for app policy (welcome email, trial grant, audit log). A hook fires
uniformly across every path that creates or authenticates a user.

::: crudauth.hooks.AuthHooks

::: crudauth.hooks.HookContext
